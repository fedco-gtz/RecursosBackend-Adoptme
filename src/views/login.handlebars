<body class="bodyRegistrarse">
    <header class="headerRegistrarse">
        <a class="anclaLogo" href="/">
            <span>INICIO</span>
        </a>
    </header>

    <main class="seccionRegistrarse">
        <form id="loginForm" action="/api/sessions/login" method="post" onsubmit="return validateForm(event)">
            <h2 class="tituloRegistrarse">Iniciar Sesión</h2>
            <div>
                <input type="email" placeholder="Email" id="email" name="email" required>
            </div>
            <div>
                <input type="password" placeholder="Contraseña" id="password" name="password" required>
            </div>
            <div>
                <input class="boton" type="submit" value="Iniciar Sesión">
            </div>
            <div>
                <a class="botonGithub" href="/api/sessions/github">Github</a>
            </div>
            <div class="help">
                <p>¿Aún no estás registrado?</p>
                <a class="helpLink" href="/register">Registrarse</a>
            </div>
        </form>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>
        async function validateForm(event) {
            event.preventDefault();

            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;

            try {
                const response = await axios.post('/api/sessions/login', {
                    email,
                    password
                });

                if (response.status === 200) {
                    alert('Inicio de sesión exitoso'); // Borrar
                    window.location.href = '/';
                }
            } catch (error) {
                if (error.response) {
                    alert(error.response.data.error);
                } else {
                    alert('Hubo un error en la solicitud'); // Borrar
                }
            }
        }
    </script>
</body>

<style>
    * {
        font-family: 'League Spartan', sans-serif;
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    .bodyRegistrarse {
        height: 100vh;
        background: linear-gradient(to right top, #24242473, #24242473), url(../../images/pages/frontPage.png);
        background-size: cover;
        background-position: center;
    }

    .headerRegistrarse {
        color: #ffffff;
        background: rgb(35, 36, 36);
        padding: 10px;
        display: flex;
        align-items: center;
        height: 50px;
    }

    .anclaLogo {
        text-decoration: none;
        color: #ffffff;
        font-weight: 700;
        margin: 30px;
    }

    .anclaLogo:hover {
        text-decoration: none;
        color: #FFB84D;
        border-bottom-style: solid;
        border-bottom-color: #FFDE59;
    }

    .seccionRegistrarse {
        background-color: rgb(36, 36, 36);
        width: 30%;
        margin: 10px auto;
        border-radius: 5px;
        padding: 40px 20px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 90vh;
        font-size: 1.2rem;
    }

    .tituloRegistrarse {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 20px;
        color: #ffffff;
    }

    form {
        width: 100%;
        padding: 40px;
    }

    input {
        width: 100%;
        margin-bottom: 20px;
        height: 40px;
        padding: 0px 20px;
        font-size: 1.2rem;
        border-radius: 20px;
        border: 2px solid #FFB84D;
        background-color: #333333;
        outline: none;
        color: rgb(255, 255, 255);
    }

    .boton {
        width: 87%;
        box-sizing: content-box;
        background-color: #FFB84D;
        border: 2px solid #FFB84D;
        color: #333333;
        padding: 1rem 1rem;
        border-radius: 5px;
        font-size: 1.5rem;
        font-weight: 700;
    }

    .botonGithub {
        width: auto;
        box-sizing: content-box;
        background-color: rgba(0, 0, 0, 0.87);
        border: 2px solid rgba(0, 0, 0, 0.87);
        color: #ffffff;
        padding: 5px;
        border-radius: 5px;
        font-size: 1.5rem;
        font-weight: 700;
        text-decoration: none;
    }

    .botonGithub:hover,
    .botonFacebook:hover,
    .boton:hover {
        border: 2px solid rgba(255, 255, 255, 0.87);
    }

    .botonFacebook {
        width: auto;
        box-sizing: content-box;
        background-color: rgba(59, 89, 152, 0.87);
        border: 2px solid rgba(59, 89, 152, 0.87);
        color: #ffffff;
        padding: 5px;
        margin 5px;
        border-radius: 5px;
        font-size: 1.5rem;
        font-weight: 700;
        text-decoration: none;
    }

    .help {
        padding-top: 20px;
        font-size: 1rem;
        text-decoration: none;
        color: #ffffff;
        font-weight: 700;
    }

    .helpLink {
        color: #787878;
    }

    .helpLink:hover {
        color: #FFDE59;
        font-weight: 700;
    }
</style>